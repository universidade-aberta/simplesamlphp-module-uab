{% set pagetitle = '%main Account Expired or Disabled'|trans({"%main": MainAccountProvider}) %}

{% extends "base.twig" %}

{% block postload %}
    {#<script src="{{ asset('js/loginuserpass.js', 'core') }}"></script>#}
{% endblock %}

{% block content %}
    
    <h2 class="page-title">{{ '%main Account Expired or Disabled'|trans({"%main": MainAccountProvider}) }}</h2>

    <p>{{ 'The %main account for "%primaryAccount" is disabled or it has expired. Please, contact technical support if you think this is an error.'|trans({"%main": MainAccountProvider, "%primaryAccount": PrimaryAccount}) }}</p>
    
{% endblock %}
